'use client'

// GoogleSignInButton removed per project decision to use Supabase-only auth.
// This file intentionally left as a stub to avoid import errors.

export function GoogleSignInButton() {
  // No-op stub
  return null
}
