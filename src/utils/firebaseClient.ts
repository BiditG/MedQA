'use client'

// firebaseClient removed — project uses Supabase-only auth now.
// Keep a stub to avoid breaking imports if any remain elsewhere.

export async function getFirebaseAuth() {
  throw new Error('Firebase auth removed. Use Supabase auth instead.')
}
